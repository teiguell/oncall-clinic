import { useTranslation } from 'react-i18next';

const LanguageSwitcher = () => {
  const { t } = useTranslation();

  return null; // No renderizamos nada ya que solo usaremos español
};

export default LanguageSwitcher;