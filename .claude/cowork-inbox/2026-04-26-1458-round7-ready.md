# Round 7 listo — UX Batch M1-M12

**Tipo**: task
**Prioridad**: media
**Espera respuesta en outbox**: sí (ack al inicio + commit SHAs al final)

## TL;DR

12 mejoras UX agrupadas en 1 deploy. 6 son gaps detectados live por Cowork (M1-M6), 6 son follow-ups Sprint 6 con componentes ya existentes que solo necesitan wire (M7-M12).

Mega-prompt completo: [`PROMPT_ROUND7_UX_BATCH.md`](../../PROMPT_ROUND7_UX_BATCH.md) en raíz del repo.

## Gaps detectados live (Cowork 26-abr-2026)

- **M1**: Doctor card no muestra distancia km ni ETA (filtro "Más cercanos" inservible)
- **M2**: Botón "Continuar con Dr. Carlos" ambiguo cuando hay 2 médicos con mismo nombre
- **M3**: Sin distancia visible, ordering de cards no se entiende
- **M4**: CTA "Continuar" en Step 2 NO sticky mobile — requiere scroll
- **M5**: Mapa Step 3 placeholder, no Google Maps real
- **M6**: Chips síntomas sin iconos — poco escaneable mobile

## Sprint 6 follow-ups (componentes listos, solo wire)

- **M7**: BottomTabBar wired en `patient/layout.tsx` y `doctor/layout.tsx`
- **M8**: ServiceTimeline embebido en landing + tracking
- **M9**: BookingFaq embebido en Step 4 confirm
- **M10**: 12 `animate-spin` residuales → skeletons (mantener Loader2 en botones)
- **M11**: useOptimistic en booking submit + chat send + doctor accept
- **M12**: Componente `<DistanceBadge>` reutilizable (cubre M1)

## Reglas

- Splitear en 3-5 commits temáticos (no monolítico)
- NO tocar archivos off-limits del CLAUDE.md R4
- Verificación obligatoria: bundle hash cambió + `.map` HTTP 200 + Playwright/`--disable-extensions` test
- Reporte completo en CODE_LOG.md siguiendo template del Round 7 Section 5

## Si te bloquea algo

Si encuentras una decisión que no está en el prompt (ej. ¿incluir M5 Google Maps requiere env var nueva en Vercel? si no la hay, ¿degrade a placeholder?), responde en outbox antes de aplicar.
