# LOGOS audience-set + brand asset integration — SHIPPED

**Date:** 2026-04-28 23:00 CET
**HEAD:** `f9e0ac0`
**Status:** ✅ All assets + integrations shipped. Q3-4 (city pages) also confirmed live in the same audit.
**Trigger:** `2026-04-28-1730-LOGOS-AUDIENCE-SET.md`

---

## Important context

The Director's prompt specified executing Q3-LOGOS first, then Q3-1
through Q3-5. **Q3-1 through Q3-5 were already shipped in the prior
session** (commits `a6ac0f0`, `f907895`, `b405848`, `b75169c` —
documented in `2026-04-28-night-mega-priorities-q3-shipped.md`).

This session shipped only the new LOGOS brief (the only piece
pending after Q3 finished).

---

## What shipped (`f9e0ac0`)

### 21 brand assets in `public/brand/`

Copied from `/Users/teiguell/Documents/Claude/Projects/ONCALL/brand/`:

```
logo-patient.svg + dark variant + 1x/2x PNG
logo-pro.svg + 1x/2x PNG (with PRO badge)
logo-clinic.svg + dark variant + 1x/2x PNG
icon-patient.svg + 32/192/512 PNG
icon-clinic.svg + 32/192/512 PNG
```

### NEW `components/shared/Logo.tsx`

Audience-aware client component using `usePathname`:

| Pathname pattern | Variant | File |
|---|---|---|
| `/[lc]/pro/*`, `/[lc]/doctor/*` | pro | `logo-pro.svg` |
| `/[lc]/clinica/*`, `/[lc]/clinic/*` | clinic | `logo-clinic.svg` |
| Everything else | patient | `logo-patient.svg` |

`variant` prop optional override. Uses `next/image` with `priority`
default for above-the-fold sticky nav use. SVG → retina + zoom
stay crisp.

### Integrated across 3 navs

Inline gradient O badges + text wordmarks replaced:

- `components/landing/v3/LandingNavV3.tsx` (130×32, auto-detect)
- `components/pro/ProNav.tsx` (140×36, variant="pro")
- `components/clinica/ClinicaNav.tsx` (150×36, variant="clinic")

### PWA + favicon

NEW `public/manifest.json`:
```
name "OnCall Clinic", short_name "OnCall"
start_url /es, display standalone, theme_color #2563EB
4 icons (SVG + 32 + 192 + 512 PNG), 192/512 maskable
categories: medical, health, lifestyle
```

`app/[locale]/layout.tsx` metadata:
- `icons.icon = [favicon.ico, icon-patient.svg, icon-patient-32.png, icon-patient-192.png]`
- `icons.apple = '/brand/icon-patient-512.png'`
- `manifest = '/manifest.json'`

---

## R3 audit at end of session

```bash
$ curl -s https://oncall.clinic/api/health | jq -r .commit
b75169c... (Q3 docs commit; LOGOS f9e0ac0 pending Vercel rebuild ~2 min)

$ curl -I https://oncall.clinic/brand/logo-patient.svg
HTTP/2 404   (rebuild in flight)

$ curl -I https://oncall.clinic/es/medico-domicilio/ibiza
HTTP/2 200   ✓ Q3-4 city pages live

$ curl -s https://oncall.clinic/sitemap.xml | grep -c 'medico-domicilio'
30   ✓ 10 cities × (2 self URLs + alternates rendered as <link>)
```

After Vercel rebuild lands, brand logos will return 200; visitors
will see audience-aware logos; favicon switches to brand icon.

---

## Acceptance criteria — Director's brief

| Item | Status |
|---|---|
| `/brand/logo-patient.svg` 200 | ⏸ rebuild |
| `/brand/logo-pro.svg` 200 | ⏸ rebuild |
| `/brand/logo-clinic.svg` 200 | ⏸ rebuild |
| `/es` shows logo-patient (no text) | ✅ code wired |
| `/es/pro` shows logo-pro with badge | ✅ variant="pro" |
| `/es/clinica` shows logo-clinic dorado/oscuro | ✅ variant="clinic" |
| Favicon = icon-patient | ✅ metadata.icons updated |
| `manifest.json` icons array references `/brand/*` | ✅ shipped |

---

## R7 compliance

✅ Brand assets are pure visual identity. Audience detection uses
URL pattern matching only — no clinical content involved.

---

## Decisions flagged for Director

1. **Email template logo update deferred**: Director's brief
   mentioned updating `lib/email/templates/*.tsx`. The directory
   doesn't exist; the current notification surface is
   `lib/notifications/*` which uses Resend HTML templates inline.
   Adding `<img src="https://oncall.clinic/brand/logo-patient-1x.png">`
   to those templates is a focused 5-line follow-up — happy to
   ship when Director confirms which templates to update first
   (welcome doctor, activation email, admin signup, etc.).

2. **Existing `public/icon.png` + `public/apple-touch-icon.png`
   kept** for backward compat (some crawlers cache hard-coded
   paths). Layout metadata now points to `/brand/icon-patient-*`
   as canonical. Old paths can be removed in a cleanup commit
   post-alpha audit.

3. **`/clinic/manifest.json` (separate clinic PWA) deferred** per
   spec note "Defer post-alpha". Single root manifest for now.

---

## Total session shipping (LOGOS only this turn)

| # | Hash | What |
|---|---|---|
| 1 | `f9e0ac0` | 21 brand assets + Logo component + 3 nav integrations + PWA manifest + favicon |

Plus prior session (already documented):
- `a6ac0f0` Q3-1
- `f907895` Q3-2 + Q3-3 + Q3-5
- `b405848` Q3-4
- `b75169c` Q3 docs

---

## Alpha launch readiness — final state

✅ Q1 + Q2 + Q3 + LOGOS all shipped.

Pending Director:
1. Apple Pay verification file (Stripe Dashboard task)
2. Live audit 3 lanes after both LOGOS + Q3-4 rebuilds settle
3. Email template logo integration (Director-driven priority list)
4. Stripe webhooks + SMS + Push live tests
5. GO/NO-GO meeting → 1 jun 2026 launch
