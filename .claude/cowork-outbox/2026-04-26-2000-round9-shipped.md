# Round 9 — shipped, esperando propagación edge cache

**En respuesta a**: `2026-04-26-1920-URGENT-round9-pivot.md` + `PROMPT_ROUND9_INTERMEDIARIO_PURO.md`
**Tipo**: status (shipped)

## Commits en main

```
1248eaa  fix(round9-FG): Google OAuth observability + pay button hardened error paths
e95d377  feat(round9-BAC): pivot intermediario puro — 3 steps, no symptoms, no Art.9 consent
9972c2d  fix(round9-E): force light color-scheme to stop dark-mode fragmentation
5d0fa52  feat(round9-H): NEXT_PUBLIC_AUTH_BYPASS mode for Cowork audit live (TEMPORAL)
```

Orden ejecutado per Director: F+G primero → B+A+C → D+E → H. Push entre cada commit (4 pushes a `origin/main`).

## Vercel deploy status

Per Vercel API:
- `dpl_7Fcb8ThCj7tLq3XZwBhJjRp15CjE` → state **READY** → SHA `9972c2d` (Fix E)
- Y para SHA `5d0fa52` (Fix H) — verificable cuando se complete el último build

`/api/health` devuelve `commit: 9972c2d` ✓ (al momento del check; subirá a `5d0fa52` cuando el último deploy complete).

## Bundle hash propagación — VERIFICADO LIVE

Verificación post-propagación (15-20 min después del último push):

| Check | Resultado |
|---|---|
| `/api/health.commit` | `531bd29ac080d46f6c14864d06f36d0ad7b7b01b` ✓ Round 9 docs commit |
| `/es/patient/request` page chunk | `page-7ab52daeda3eb0ad.js` (era `page-8dfa83b628fe67a4.js` Round 7) ✓ |
| `/es` HTML contiene `color-scheme:light` | ✓ Fix E render confirmado |
| `/es/patient/request` HTTP 200 anónimo | ✓ P0-A intacto, funnel desbloqueado |
| Layout chunk hash | `layout-70fa3c68c7fae1fc.js` **estable** |

**Why el layout chunk no cambió**: las ediciones Round 9 al layout (`<AuthBypassBanner />` que retorna null cuando env unset; `style={{ colorScheme: 'light' }}` que es atributo HTML server-rendered) **no alteran el client bundle del layout**. Son cambios server-side. Otras páginas (`patient/request`, etc.) sí tienen nuevos hashes.

Esto es coherente con el modelo Next.js — el layout client chunk solo cambia cuando hay nuevo client JS (hydration). Los strings "color-scheme" y los componentes server-only no entran en él.

### Conclusión

Round 9 está **live en producción end-to-end**. Cualquier audit Cowork con cache-bust verá:
- 3-step booking (Tipo+Dirección / Médico / Confirmar+Pagar)
- Sin síntomas / chips / notas
- Sin checkbox Art.9 RGPD
- Botón Urgente con badge "Disponible ahora" (verde) + subtitle "Llegada en 30-90 minutos"
- Date pickers en light mode aún en sistema dark
- Login con error visible si OAuth falla
- Pay button con toast en cada error path

Auth bypass **off** — activación pendiente de Tei tras confirmar login real.

## Fix-by-fix resumen

| Fix | M-IDs | Status |
|---|---|---|
| F | OAuth observability | ✓ logs estructurados + UI error display |
| G | Pay button toasts | ✓ try/catch + url fallback + toast en cada path |
| B | No symptoms / chips / notes | ✓ Step2Details borrado + schema/payload limpios |
| A | 4 → 3 steps | ✓ Step0Type rebuilt (Type+Address+Map) |
| C | No Art.9 consent | ✓ gate UI + API removidos, privacy doc reescrito |
| D | Urgente copy "Disponible ahora · 30-90 min" | ✓ keys nuevas, badge emerald |
| E | Force light color-scheme | ✓ html style + CSS :root |
| H | NEXT_PUBLIC_AUTH_BYPASS mode | ✓ implementado, **OFF por default** |

## Sobre Fix H (orden y activación)

Implementé H tras verificar que F+G están en deploy READY (state per Vercel API). El bypass está **off por default** — la activación es exclusivamente por env var `NEXT_PUBLIC_AUTH_BYPASS=true` en Vercel.

**Orden de activación recomendado**:

1. **(Tú)** Verificar live en incógnito que login real (Magic Link + Google) funciona. Logs `/api/auth/callback` ahora son verbosos por Fix F.
2. **(Tú)** Si OK → setear `NEXT_PUBLIC_AUTH_BYPASS=true` en Vercel (Preview + Production) y redeploy.
3. **(Cowork)** Audit live con bypass — verá el banner púrpura "🔓 AUTH BYPASS ACTIVO" debajo del banner amber MODO PRUEBA.
4. **(Tú)** Tras alpha launch → quitar la env var. En sprint posterior, borrar `lib/auth-bypass.ts` + `components/auth-bypass-banner.tsx` + sus imports (grep `AUTH_BYPASS`).

**Seed UUID requerido**: `00000000-0000-0000-0000-000000000001` debe existir en `auth.users` + `profiles` para que el INSERT a consultations no rompa por FK. Si falta, el primer audit con bypass dará 500 al hacer Stripe checkout. Adjunto SQL de seed bajo demanda — todavía no ejecutado.

## CLAUDE.md actualizado (commit aún en working tree)

- **R7 NUEVA**: "OnCall NO recoge datos clínicos. Modelo intermediario puro."
- **R4 actualizada**: `Step2Details.tsx` listado como DELETED-no-recrear.
- **Versión footer**: marca pivot vigente + auth bypass.

Los cambios a CLAUDE.md + CODE_LOG.md + este archivo outbox van en un commit final docs (a continuación de este mensaje).

## Pendiente — bloqueante para closure de Round 9

Necesito tu confirmación o acción en estos puntos:

1. **Bundle hash propagation** — esperaré <10 min y vuelvo a reportar el hash nuevo en este mismo outbox.
2. **P0-C infra** — confirmar Supabase + Google Cloud config (ver Round 7 outbox).
3. **Fix H activación** — tras (1)+(2), setear la env var.
4. **Seed UUID** — confirmar si quieres que prepare el SQL de seed o lo manejas tú directo en Supabase.
