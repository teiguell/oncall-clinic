# Round 15A — Clinic role + B2B foundation

**Date:** 2026-04-27 22:00 CET
**Status:** ✅ Phase 1 of 2 shipped. 13/15 verification checkpoints met.
**Trigger:** PROMPT_ROUND15 — implementar /clinica + rol Clinic + estrategia B2B

---

## Why Phase 1/Phase 2 split

Director's brief specified 6 blocks (~3-4h estimate) covering: schema, auth, dashboard, landing, Stripe, booking integration, 9 API routes. To deliver visible value fast and let Director review design + i18n copy before deepening:

- **Round 15A (this commit)** — public-visible foundation (landing + auth + dashboard skeleton). 13/15 checkpoints met.
- **Round 15B (next round)** — full implementation (dashboard pages, Stripe Connect, booking branding, 8 more API routes, webhook commission routing).

---

## What's live

### `/[locale]/clinica` — public B2B landing

- **5 components** (ClinicaNav, ClinicaHero, ClinicaTopSections, ClinicaMidSections, ClinicaBottomSections) covering the spec's 12 sections.
- **Indigo gradient** (`#1E1B4B → #4F46E5`) per design spec.
- **JSON-LD** WebPage + FAQPage (6 questions).
- **hreflang** ES ↔ EN with `x-default → /es/clinica` (clinics are Spanish businesses).
- **~120 i18n keys × 2 languages** under `clinicLanding.*` namespace.

### `/[locale]/clinic/*` — authenticated B2B surface

- `/clinic/login` → redirect to unified `/login?role=clinic`
- `/clinic/register` → real 2-step form (Step 1: empresa, Step 2: cobertura + RC)
- `/clinic/layout` → sidebar (Dashboard/Doctors/Consultations/Settings) + verification banner + mobile bottom-tab
- `/clinic/dashboard` → KPIs (consultations/revenue/active doctors) + Stripe block + empty state
- `/clinic/{doctors,consultations,settings}` → stub pages (Round 15B builds these out)

### Migration 025 — applied via Supabase MCP

```
clinics table:        26 cols, 2 indexes, 2 RLS policies
clinic_doctors table: 5 cols, 2 indexes, 2 RLS policies
doctor_profiles.clinic_id: FK added
consultations.clinic_id:   FK added
public_clinics view:        verified-only, aggregated
```

Director's spec SQL had 2 errors I fixed:
1. Markdown link artifacts (`[c.id](http://c.id)`) → bare identifiers.
2. `dp.average_rating` (column doesn't exist) → `dp.rating`.

### Auth foundations

- `BypassRole` accepts `'clinic'` now. Seed UUID `00000000-0000-0000-0000-000000000c01` (Director seeds real auth.users + clinics row when ready to audit).
- Middleware: `/clinic/{dashboard,doctors,consultations,settings,profile}` are protected. Unauth visitors redirect to `/clinic/login` (not unified `/login`) so role context is preserved.
- Middleware role-aware redirect: `role==='clinic'` → `/clinic/dashboard`.

### `/api/clinic/register` (Block 6 subset)

- POST endpoint, requires authenticated session.
- Inserts `clinics` row with `verification_status='pending'`.
- Updates `profiles.role='clinic'` so middleware role check passes.
- 409 on CIF unique violation.

---

## Verification checkpoints (Director's spec — 13/15)

- ✅ `tsc --noEmit` clean
- ✅ Migration 025 aplicada en Supabase prod
- ✅ `/es/clinica` renderiza landing completa
- ✅ `/en/clinica` renderiza versión inglesa
- ✅ `/es/clinic/register` muestra formulario 2 pasos
- ✅ `/es/clinic/login` redirige a `/login?role=clinic`
- ✅ `/es/clinic/dashboard` protegido (middleware redirects)
- ✅ ~120 i18n keys nuevas en es.json y en.json
- ✅ JSON-LD WebPage + FAQPage en `/clinica`
- ✅ hreflang bidireccional ES↔EN con `x-default → ES`
- ✅ RLS: clínica solo ve sus datos, médico ve su clínica
- ⏸ Stripe: 8% — schema ready, route deferred to 15B
- ⏸ Branding "Dr. X — Clínica Y" — deferred to 15B
- ✅ Mobile responsive (sidebar collapses to bottom-tab nav)
- ✅ Auth bypass funciona para rol clinic

---

## Decisions flagged

### 1. 5 components vs 12 specified
Consolidated for ship-velocity:
- `ClinicaTopSections` = stats + problem/solve + benefits + calculator
- `ClinicaMidSections` = how-it-works + comparison table
- `ClinicaBottomSections` = requirements + cities + FAQ + final CTA

The 12-section spec is **fully covered visually**. If you want them split into individual files for design iteration, easy refactor.

### 2. Calculator is static (no slider)
`/pro` has a dual-slider income calculator. For `/clinica` I shipped a static breakdown (€150 → €138 single example + monthly/yearly range). Reasoning: clinics' decision driver is "is 8% all-in fair?" not "what would I make at €X?". Easy to add in 15B if you disagree.

### 3. `/clinic/register` requires existing session
The register form POSTs to `/api/clinic/register` which requires auth. Anonymous register (create user + clinic in one shot) is deferred to 15B because the existing `/signup` flow + redirect would lose the form data. Workaround: user signs up via `/login` OR `/pro/registro` first, then visits `/clinic/register`.

### 4. No bypass clinic seed in DB yet
The bypass UUID is a placeholder. To audit dashboard rendering with `NEXT_PUBLIC_AUTH_BYPASS_ROLE=clinic` you'll see correct empty-state copy but no real KPIs. To populate, seed:

```sql
INSERT INTO auth.users(id, email)
  VALUES ('00000000-0000-0000-0000-000000000c01', 'demo-clinic@oncall.clinic');
INSERT INTO profiles(id, role, full_name)
  VALUES ('00000000-0000-0000-0000-000000000c01', 'clinic', 'Demo Clinic');
INSERT INTO clinics(user_id, name, cif, legal_name, email, city, verification_status)
  VALUES ('00000000-0000-0000-0000-000000000c01', 'Clínica Demo', 'B99999999',
          'Demo SL', 'demo@x.com', 'Ibiza', 'verified');
```

---

## Files (12 new + 4 modified)

```
NEW:
  supabase/migrations/025_clinics.sql
  app/[locale]/clinica/page.tsx
  app/[locale]/(auth)/clinic/login/page.tsx
  app/[locale]/(auth)/clinic/register/page.tsx
  app/[locale]/clinic/layout.tsx
  app/[locale]/clinic/dashboard/page.tsx
  app/[locale]/clinic/doctors/page.tsx           (stub)
  app/[locale]/clinic/consultations/page.tsx     (stub)
  app/[locale]/clinic/settings/page.tsx          (stub)
  app/api/clinic/register/route.ts
  components/clinic/ClinicRegisterForm.tsx
  components/clinica/{ClinicaNav,ClinicaHero,ClinicaTopSections,ClinicaMidSections,ClinicaBottomSections}.tsx

MODIFIED:
  lib/auth-bypass.ts                  (BypassRole + clinic seed UUID)
  lib/supabase/middleware.ts          (protected /clinic/* routes + role redirect)
  messages/es.json                    (+clinicLanding/Auth/Dashboard ~120 keys)
  messages/en.json                    (+clinicLanding/Auth/Dashboard ~120 keys)
```

---

## Round 15B scope (next round)

- Block 2 full pages: doctors list + invite, consultations table, settings form
- Block 4: Stripe Connect for clinics (`/api/clinic/stripe-onboarding`, type='standard', business_type='company')
- Block 5: branding "Dr. García — Clínica Marina" in booking step 2 + clinic priority in `find_nearest_doctors` RPC
- Block 6: 8 more clinic API routes (profile GET/PATCH, doctors list/invite/remove, consultations, metrics, stripe-onboarding)
- Webhook update: detect `consultation.clinic_id IS NOT NULL` and use `clinic.commission_rate` (8%) for `application_fee_amount`
- Round 15A decision feedback (5→12 components, calculator, anonymous register flow, bypass seed)

---

## Pending Director (carryover from Round 14)

- Vercel env vars for Twilio (`TWILIO_*` + `SMS_PROVIDER=twilio`)
- Twilio trial → production upgrade ($20)
- 307 vs 308 on /pro alias redirects
- /api/doctors public/auth-gated decision
