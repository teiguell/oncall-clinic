# Round 7 — ack

**En respuesta a**: `2026-04-26-1458-round7-ready.md` + `PROMPT_ROUND7_UX_BATCH.md`
**Tipo**: ack

## Confirmado

- Leí `CLAUDE.md` (R1-R6 hard rules + R4 off-limits list).
- Leí inbox `2026-04-26-1458-round7-ready.md`.
- Leí mega-prompt `PROMPT_ROUND7_UX_BATCH.md` completo (M1-M12).

## Plan de ejecución (5 commits temáticos)

| Commit | Scope | M-IDs |
|---|---|---|
| A | DistanceBadge + ETA helper + doctor card wire | M1, M3, M12 |
| B | Step 2 sticky CTA + button rename + Step 3 Google Maps + symptom icons | M2, M4, M5, M6 |
| C | BottomTabBar + ServiceTimeline + BookingFaq wires | M7, M8, M9 |
| D | Spinners → skeletons | M10 |
| E | Optimistic UI: booking + chat + doctor accept | M11 |

## Pre-flight inventory pendiente

Antes de tocar código, confirmaré (no requiere respuesta del Director, lo verifico yo):
1. Existencia de `BottomTabBarWrapper`, `ServiceTimeline`, `BookingFaq` (Sprint 6 dice creados).
2. `@vis.gl/react-google-maps` en `package.json`.
3. `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` en `.env.local` (la prod env var es responsabilidad de Tei en Vercel).
4. `find_nearest_doctors` RPC ya devuelve `distance_km` (Round 3 lo añadió). ETA se computa client-side con `lib/eta.ts` nuevo.

Si encuentro que un Sprint 6 component **no existe** y solo está en changelog: lo creo desde cero siguiendo el spec del prompt y lo reporto en CODE_LOG.

## Reglas que respetaré

- R1: RCA antes de patch (aquí ya hay especificación detallada → no requiere reproducción manual de bug, son features).
- R2: bundle hash check post-deploy.
- R3: live audit con Playwright + system Chrome `--disable-extensions`.
- R4: off-limits files (cookie-consent[-loader], booking-store, auth.store, referral-card, login page) — **no se tocan**.
- R5: `productionBrowserSourceMaps: true` se queda activo.
- R6: smoke test de las rutas críticas tras deploy.

## ETA

Trabajo en paralelo donde sea posible. Reporte en `CODE_LOG.md` Round 7 al cerrar.
