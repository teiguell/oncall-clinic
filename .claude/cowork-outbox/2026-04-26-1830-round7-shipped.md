# Round 7 — shipped + verified

**En respuesta a**: `2026-04-26-1734-URGENT-round7-additions.md` + `PROMPT_ROUND7_UX_BATCH.md`
**Tipo**: status (shipped)

## Commits en main

```
63f8831  fix(round7-p0-a): revert Round 5 mount-time auth redirect on /patient/request
fc3163d  feat(round7-A): DistanceBadge + ETA helper + expandable doctor cards
d806b51  feat(round7-B): formatDoctorShortName + sticky Step 2 CTA + Google Map + chip icons + P1-D copy
78fce76  feat(round7-C): BottomTabBar + ServiceTimeline wires (M7, M8)
5f95657  refactor(round7-D): unify spinner pattern to <Loader2> (M10)
89d28d7  feat(round7-E): optimistic UI for chat send + doctor accept + booking submit (M11)
```

`git push origin main`: `033ee21..89d28d7` (en realidad varias series de pushes intermedios — pushed entre commits según pediste).

## Bundle live

| | Hash |
|---|---|
| Anterior (Round 6) | `layout-bd0804a56692286d.js` |
| Nuevo (Round 7) | **`layout-70fa3c68c7fae1fc.js`** |

Sourcemap `.map` HTTP 200 ✓ (sourcemaps siguen activos desde Round 6).

## P0-A diff key (lo más urgente)

`app/[locale]/patient/request/page.tsx`:

```diff
- // Round 5 Fix B — redirect anónimo a /login al MOUNT
- if (!user) {
-   const here = ...
-   router.replace(`/${locale}/login?next=${encodeURIComponent(here)}`)
-   return
- }
+ // Round 7 P0-A — solo resuelve user, NO redirect.
+ // Step3Confirm widget gestiona auth inline antes del Stripe redirect.
```

También se eliminó el `if (authChecking) return <Skeleton>` que solo existía para enmascarar el flicker del redirect.

**Verificado live** (Playwright + system Chrome, mobile UA, ventana fresca sin cookies):

- `https://oncall.clinic/es/patient/request` → final URL = misma ruta (NO redirige a /login) ✓
- Step 0 visible (Urgente / Programada) ✓
- 0 console errors

## P0-B diff key

`lib/doctor-format.ts` (NEW):

```ts
const PREFIXES = /^(Dr\.|Dra\.|Doctor|Doctora|Mr\.|Mrs\.|Ms\.)\s+/i

function clean(part: string): string {
  return (part || '').replace(PREFIXES, '').trim()
}

export function formatDoctorShortName(d: { firstName: string; lastName: string }): string {
  const first = clean(d.firstName)
  const last = clean(d.lastName)
  if (!first && !last) return ''
  if (!first) return last
  if (!last) return first
  return `${first.charAt(0).toUpperCase()}. ${last}`
}

export function formatFullNameShort(fullName: string | null | undefined): string {
  // Splits "Carlos Smith Ruiz" → first="Carlos Smith", last="Ruiz"
  // Strips PREFIXES from both before joining.
  ...
}
```

`Step1Doctor.tsx`:

```diff
- {tBooking('continueWith')} Dr. {(selectedDoctorName || '').split(' ')[0]}
+ {tBooking('continueWithDoctor', { name: formatFullNameShort(selectedDoctorName) })}
```

i18n key `continueWithDoctor`: `"Continuar con Dr. {name}"` / `"Continue with Dr. {name}"`. Si seed dice `"Dr. James Smith"`, output es `"Continuar con Dr. J. Smith"` (no doble "Dr.").

Tech debt sugerido: añadir validación de input en doctor onboarding para impedir que `first_name`/`last_name` lleven prefijos. Dejado fuera de scope porque toca form + DB; lo flagueamos para sprint posterior.

## P1-D copy nuevo

`messages/es.json`:
```json
"urgentDesc": "Médico en tu puerta en 1-3 horas según tráfico de Ibiza",
"urgentBadgeShort": "Respuesta <20 min"
```

`messages/en.json`:
```json
"urgentDesc": "Doctor at your door in 1–3 hours depending on Ibiza traffic",
"urgentBadgeShort": "Reply <20 min"
```

`Step0Type.tsx`: `&lt; 20 MIN` hardcoded → `t('request.urgentBadgeShort')`.

Lectura ahora coherente: badge = response-time, descripción = arrival-window.

## Live audit (Round 7 deploy)

| Ruta | console errors | data-dgst markers | Notas |
|---|---|---|---|
| `/es` | 0 | none | ServiceTimeline visible (regex DOM detecta los 6 steps) |
| `/es/login` | 0 | none | |
| `/es/doctor/login` (→ /login?next=…) | 0 | none | server gate doctor/layout.tsx OK |
| `/es/patient/dashboard` (→ /login?next=…) | 0 | none | server gate patient/layout.tsx OK |
| `/es/patient/request` anónimo | 0 | none | **NO redirect** ✓ Step 0 visible |

## P0-C — bloqueado por infra (no es code)

Magic Link + Google OAuth no funcionan probablemente por:

1. **Supabase SMTP**: configurar Resend en Auth → SMTP Settings (env `RESEND_API_KEY` ya en repo).
2. **Supabase Auth → URL Configuration**: Site URL = `https://oncall.clinic`, Redirect URLs incluir `https://oncall.clinic/api/auth/callback*`.
3. **Google Cloud Console → OAuth 2.0 Client → Authorized redirect URIs**:
   - `https://oncall.clinic/api/auth/callback`
   - `https://<project>.supabase.co/auth/v1/callback`

Si `redirect_uri_mismatch` aparece → 3.b. Si `error sending email` → 1+2. ¿Confirmas que se aplican?

## Lo que queda fuera

- **P1-E expandible**: implementado pero versión mínima (chevron + bio + distancia + reviews count + idiomas estáticos ES/EN). No incluido todavía: foto profesional grande, horarios disponibles hoy, reviews recientes (3). Si los necesitas, lo abro en Round 8.
- Todos los archivos de la R4 off-limits intactos: cookie-consent[-loader], booking-store, auth.store, referral-card, login page (la única excepción del login es la callbackUrl IIFE → fn que hicimos en Round 6, ya commited).
